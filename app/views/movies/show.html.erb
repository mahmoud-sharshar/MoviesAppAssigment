<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div class="movie-button-group">
  <%= link_to "Back to movies", movies_path, class: "movie-button" %>
  <%= button_to "Destroy this movie", @movie, method: :delete, class: "delete-movie-button" %>
</div>
